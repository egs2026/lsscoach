import React from 'react'
import ReactDOM from 'react-dom/client'
import LSSMasterCoach from './LSSMasterCoach'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LSSMasterCoach />
  </React.StrictMode>,
)
